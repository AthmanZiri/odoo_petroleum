<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_date_range_list" model="ir.ui.view">
        <field name="name">date.range.list</field>
        <field name="model">date.range</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="type_id"/>
                <field name="date_start"/>
                <field name="date_end"/>
                <field name="company_id" groups="base.group_multi_company"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <record id="view_date_range_form_view" model="ir.ui.view">
        <field name="name">date.range.form</field>
        <field name="model">date.range</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <ribbon status="!active" text="Archived" color="danger" />
                    <group>
                        <field name="name"/>
                        <field name="type_id"/>
                        <field name="date_start"/>
                        <field name="date_end"/>
                        <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}"/>
                        <field name="active" invisible="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_date_range_search" model="ir.ui.view">
        <field name="model">date.range</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <filter name="date_start" date="date_start"/>
                <filter name="date_end" date="date_end"/>
                <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
                <group name="groupby">
                    <filter name="date_start_groupby" string="Start Date" context="{'group_by': 'date_start'}"/>
                    <filter name="date_end_groupby" string="End Date" context="{'group_by': 'date_end'}"/>
                    <filter name="company_groupby" string="Company" context="{'group_by': 'company_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="view_date_range_type_tree" model="ir.ui.view">
        <field name="name">Date Range</field>
        <field name="model">date.range.type</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="allow_overlap"/>
                <field name="company_id" groups="base.group_multi_company"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <record id="view_date_range_type_form_view" model="ir.ui.view">
        <field name="name">date.range.type.form</field>
        <field name="model">date.range.type</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <ribbon status="!active" text="Archived" color="danger" />
                    <notebook>
                        <page string="Configuration">
                            <group>
                                <field name="name"/>
                                <field name="allow_overlap"/>
                                <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}"/>
                                <field name="active" invisible="1"/>
                            </group>
                        </page>
                        <page string="Generation">
                            <group>
                                <field name="duration_count"/>
                                <field name="unit_of_time"/>
                                <field name="name_prefix"/>
                                <field name="name_expr"/>
                                <field name="range_name_preview"/>
                                <field name="date_ranges_exist" invisible="1"/>
                                <field name="autogeneration_date_start" modifiers="{'invisible': [('date_ranges_exist', '=', True)]}"/>
                                <field name="autogeneration_count"/>
                                <field name="autogeneration_unit"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_date_range_type_search" model="ir.ui.view">
        <field name="model">date.range.type</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
            </search>
        </field>
    </record>

    <record id="date_range_action" model="ir.actions.act_window">
        <field name="name">Date Ranges</field>
        <field name="res_model">date.range</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="date_range_type_action" model="ir.actions.act_window">
        <field name="name">Date Range Types</field>
        <field name="res_model">date.range.type</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_date_range" name="Date ranges" parent="base.menu_custom" sequence="1"/>
    <menuitem action="date_range_action" id="menu_date_range_action" parent="menu_date_range"/>
    <menuitem action="date_range_type_action" id="menu_date_range_type_action" parent="menu_date_range"/>
</odoo>
