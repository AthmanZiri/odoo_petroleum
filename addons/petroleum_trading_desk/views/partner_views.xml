<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_partner_filter_trading_desk" model="ir.ui.view">
        <field name="name">res.partner.search.trading.desk</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="account.res_partner_view_search"/>
        <field name="arch" type="xml">
            <filter name="customer" position="after">
                <filter name="customer_balance" string="Customers with balance"
                        domain="[('customer_rank', '&gt;', 0), ('credit', '&gt;', 0)]"/>
                <filter name="supplier_balance" string="Vendors with balance"
                        domain="[('supplier_rank', '&gt;', 0), ('debit', '&gt;', 0)]"/>
            </filter>
        </field>
    </record>

    <record id="view_partner_tree_trading_desk" model="ir.ui.view">
        <field name="name">res.partner.list.trading.desk</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_tree"/>
        <field name="arch" type="xml">
            <field name="complete_name" position="after">
                <field name="credit" optional="show"
                       column_invisible="context.get('res_partner_search_mode') != 'customer'"/>
                <field name="debit" optional="show"
                       column_invisible="context.get('res_partner_search_mode') != 'supplier'"/>
            </field>
        </field>
    </record>

    <record id="action_trading_desk_customers" model="ir.actions.act_window">
        <field name="name">Customers</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="domain">[('customer_rank', '&gt;', 0)]</field>
        <field name="context">{
            'search_default_customer': 1,
            'res_partner_search_mode': 'customer',
            'default_is_company': True,
            'default_customer_rank': 1,
        }</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a customer contact.
            </p><p>
                Customers appear on trading deals and customer invoices.
                The <b>Total Receivable</b> column shows the outstanding balance.
            </p>
        </field>
    </record>

    <record id="action_trading_desk_vendors" model="ir.actions.act_window">
        <field name="name">Vendors</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="domain">[('supplier_rank', '&gt;', 0)]</field>
        <field name="context">{
            'search_default_supplier': 1,
            'res_partner_search_mode': 'supplier',
            'default_is_company': True,
            'default_supplier_rank': 1,
        }</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a supplier contact.
            </p><p>
                Vendors are assigned on deal product lines and vendor bills.
                The <b>Total Payable</b> column shows the outstanding balance.
            </p>
        </field>
    </record>
</odoo>
